/**
 * Default styling fallbacks used when a task's category key
 * is not found in the roadmap's categories object.
 * @see Requirements 3.7
 */

/** Default text color for tasks with unknown category keys */
export const DEFAULT_TEXT_COLOR = 'var(--color-text-secondary)';

/** Default background color for tasks with unknown category keys */
export const DEFAULT_BG_COLOR = 'var(--color-background-secondary)';
